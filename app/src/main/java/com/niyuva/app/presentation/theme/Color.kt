package com.niyuva.app.presentation.theme

import androidx.compose.ui.graphics.Color

// Core Palette
val DeepPlumRose = Color(0xFF8B5E6D)       // Primary — buttons, active states
val Terracotta = Color(0xFFD4956A)          // Accent — links, highlights, CTAs, dates
val WarmIvory = Color(0xFFFBF3EE)           // Background — all screen backgrounds
val WarmLinen = Color(0xFFF5E6DC)           // Surface — cards, input fields, chips
val DeepWarmBrown = Color(0xFF1E1418)       // Text Primary — all body text, headings
val DustyMauve = Color(0xFFA07A85)          // Text Secondary — subtitles, placeholders
val BlushMist = Color(0xFFEDD9D0)           // Divider — borders, separators
val PureWhite = Color(0xFFFFFFFF)           // Cards on phase backgrounds, splash text

// Phase Colors
val PhaseColorMenstruation = Color(0xFFC17A8A)   // Deep Rose
val PhaseColorFollicular = Color(0xFF76C4B4)     // Fresh Teal
val PhaseColorOvulation = Color(0xFFA8C5A0)      // Sage Green
val PhaseColorLuteal = Color(0xFFD4A574)         // Warm Amber

// Phase Gradient Endpoints
val PhaseGradientMenstruationEnd = Color(0xFFD4A5B5)
val PhaseGradientFollicularEnd = Color(0xFFCBEAE3)
val PhaseGradientOvulationEnd = Color(0xFFC8E0C2)
val PhaseGradientLutealEnd = Color(0xFFE8C9A0)

// Category Colors (Log Screen)
val CategorySleep = Color(0xFF8BAEC8)
val CategoryMood = Color(0xFFE8965A)
val CategoryFlow = Color(0xFFC97A8A)
val CategoryPain = Color(0xFF9B8BC4)
val CategorySexual = Color(0xFF7BBFB0)
val CategoryBirthControl = Color(0xFFC9A85C)

// Body Topic Card Colors
val TopicMyCycle = Color(0xFFD5E8D2)
val TopicHormones = Color(0xFFE8E0F0)
val TopicHygiene = Color(0xFFD2EDE8)
val TopicPeriodProducts = Color(0xFFF5E0D5)
val TopicPCOS = Color(0xFFF0E5C8)
val TopicDuringPeriods = Color(0xFFF0D5DC)
val TopicMyBody = Color(0xFFF5ECD5)
val TopicAnimations = Color(0xFFE8D8E0)

// Destructive
val DestructiveRose = Color(0xFFC97A8A)

// Disabled
val DisabledButton = Color(0xFFC5A8B3)
